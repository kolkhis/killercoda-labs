# Running Your Script

To run the script, you simply specify the path to the script file.  
Since it's in the current directory, we can use the relative path notation (`./`) before the script name:

```bash
./hello
```{{exec}}



